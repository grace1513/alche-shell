#!/bin/bash
# Shell - Init Files, Variables and Expansions

This project contains shell scripts that demonstrate
the use of init files, variables, and expansions in bash.

## Scripts

### 0-alias
Creates an alias named `ls` with the value `rm *`.
When sourced, typing `ls` will delete all files
in the current directory.

Usage:
    source ./0-aliaiis
